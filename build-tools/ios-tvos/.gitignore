# xcodebuild derived data created by refresh-profiles.sh — pure cache.
.build/

# xcodegen output. Regenerated from each yetty-*/project.yml on demand.
*.xcodeproj/

# Per-version downloaded tarballs + hand-assembled YettyQemu.app, dropped
# by tools/ios-tvos/{ios,tvos}/qemu/x86_64.sh.
.cache/
