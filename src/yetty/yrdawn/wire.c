/*
 * wire.c — runtime helpers for the WebGPU-over-OSC bridge.
 *
 * The protocol surface lives in <yetty/yrdawn/wire.h> (declarations) and
 * the codegen output (per-method args structs + dispatcher). Placeholder
 * for shared client/server plumbing that isn't codegen-emitted.
 */
#include <yetty/yrdawn/wire.h>
