/*
 * yplatform/window/default.c — REMOVED (placeholder).
 *
 * The GLFW desktop window is now the yplatform:glfw_window yclass
 * (src/yetty/yplatform/ywindow/glfw.c); the old hand-written window functions
 * that lived here are gone. This file defines no symbols — it exists only so the
 * not-yet-migrated standalone-app CMake source lists still resolve. Delete it
 * once those apps drop the reference.
 */

typedef int yetty_yplatform_window_default_removed;
