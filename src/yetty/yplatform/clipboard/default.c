/*
 * yplatform/clipboard/default.c — REMOVED (placeholder).
 *
 * The GLFW desktop clipboard is now the yplatform:glfw_clipboard yclass
 * (src/yetty/yplatform/yclipboard/glfw.c); the old clipboard-manager that lived
 * here is gone. This file defines no symbols — it exists only so the
 * not-yet-migrated standalone-app CMake source lists still resolve. Delete it
 * once those apps drop the reference.
 */

typedef int yetty_yplatform_clipboard_default_removed;
