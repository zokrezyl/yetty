/* Build-graph stub: keeps the yetty_api_ygui2 target valid when the
 * generated gen/api/ygui2 sources are absent (fresh codegen bootstrap —
 * the glob is empty until the first `make codegen` completes). */
typedef int yetty_ygui2_api_stub;
